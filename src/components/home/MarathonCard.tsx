import { Marathon } from "@/types/marathon";

interface MarathonCardProps {
  marathon: Marathon;
  onClick: () => void;
}

export default function MarathonCard({ marathon, onClick }: MarathonCardProps) {
  return <div></div>;
}
